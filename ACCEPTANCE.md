# Acceptance checklist — Bounty #4 ($40)

- [x] `build.py --dry-run` — no side effects, planned commands only
- [x] `--dry-run-format json` — structured plan output
- [x] Per-module command planning (backend cargo, engine cmake)
- [x] Clean mode support
- [x] Tests: `bash run_tests.sh` (9 unittest cases)

/claim #4 — PR ready for merge.
